{
  "name": "site-ithona-qa",
  "version": "1.0.0",
  "private": true,
  "description": "IT-HONA corporate site — QA tooling (static checks, worker unit tests, Playwright e2e)",
  "scripts": {
    "check": "node scripts/check-site.mjs",
    "test:worker": "node --test tests/worker.test.mjs",
    "test:e2e": "playwright test",
    "test": "npm run check && npm run test:worker && npm run test:server && npm run test:e2e",
    "test:server": "node --test tests/server.test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}