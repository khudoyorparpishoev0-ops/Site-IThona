# План работ по итогам QA-аудита

Формат: этапы в порядке приоритета. У каждой задачи указано, **кто** может её выполнить:
- 🧑 **Владелец** — нужны доступы (Cloudflare, домен, Telegram) или бизнес-решение;
- 🤖 **Claude** — делается в этом репозитории по запросу;
- 🤝 **Вместе** — Claude готовит, владелец применяет/проверяет.

Статусы: ☐ не начато · ☑ готово. Обновлять по мере выполнения.

---

## Этап 1 — Подключить Telegram Worker (снимает главный WARNING)

Цель: заявки с формы приходят в Telegram, а не только в WhatsApp-fallback.

| # | Задача | Кто | Статус |
|---|---|---|---|
| 1.1 | Создать бота у @BotFather → получить `BOT_TOKEN`. Создать группу для заявок, добавить туда бота, получить `CHAT_ID` (например, через @userinfobot или getUpdates) | 🧑 | ☐ |
| 1.2 | Задеплоить `worker/telegram-worker.js` в Cloudflare: `npx wrangler deploy` (или через дашборд Workers → Create → вставить код) | 🧑 | ☐ |
| 1.3 | В настройках Worker → Settings → Variables добавить **secrets** `BOT_TOKEN` и `CHAT_ID` (никогда не в код/git) | 🧑 | ☐ |
| 1.4 | Прислать URL Worker'а (вида `https://<имя>.<аккаунт>.workers.dev`) → вписать его в `CONTACTS.formEndpoint` в `assets/i18n.js`, поднять версию `i18n.js?v=6` во всех 6 HTML | 🤝 (URL — владелец, правка — Claude) | ☐ |
| 1.5 | Включить Cloudflare Rate Limiting rule на маршрут Worker (например, 5 POST/мин с одного IP) — закрывает риск R1 | 🧑 | ☐ |
| 1.6 | Контрольная заявка с прод-сайта → сообщение пришло в чат; проверить и ошибочный сценарий (выключить Worker → форма показывает понятную ошибку) | 🤝 | ☐ |

**Критерий готовности:** заявка с it-hona.tj приходит в Telegram; `curl -X POST -H "Origin: https://evil.example"` получает 403; повторный спам режется rate-limit'ом.

---

## Этап 2 — Публикация на production и настройка хостинга

Цель: сайт открыт на https://it-hona.tj со всеми серверными настройками.

| # | Задача | Кто | Статус |
|---|---|---|---|
| 2.1 | Задеплоить статику (6 HTML + 404.html + robots.txt + sitemap.xml + assets/). Не выкладывать: docs/, tests/, scripts/, worker/, node_modules/, package*.json | 🧑 | ☐ |
| 2.2 | Настроить отдачу 404.html на несуществующие URL (Cloudflare Pages — автоматически; nginx: `error_page 404 /404.html;`) | 🧑 | ☐ |
| 2.3 | Security-заголовки из `09_SECURITY_AUDIT.md` (CSP, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, frame-ancestors). Для Cloudflare Pages Claude может подготовить готовый файл `_headers` | 🤝 | ☐ |
| 2.4 | Cache-Control: HTML — короткий/no-cache; картинки — длинный; CSS/JS — длинный (версии `?v=` уже есть) | 🧑 | ☐ |
| 2.5 | Google Search Console: подтвердить домен, отправить sitemap.xml | 🧑 | ☐ |
| 2.6 | Прогнать Lighthouse (mobile+desktop) по 6 страницам — цель ≥90; прислать результаты, если ниже — Claude разберёт | 🤝 | ☐ |
| 2.7 | Смоук вручную: Safari (iPhone) и Firefox — открыть все страницы, меню, форму (в аудите NOT TESTED) | 🧑 | ☐ |
| 2.8 | Проверить соцпревью (og-image) при шаринге в Telegram/WhatsApp | 🧑 | ☐ |

**Критерий готовности:** все страницы 200 по HTTPS, /nope → брендовая 404, Lighthouse ≥90 (или объяснённые отклонения), превью корректное.

---

## Этап 2б — Корпоративная почта на домене (info@, hr@)

Цель: рабочие ящики `info@ithona.tj` (уже указан на сайте) и `hr@ithona.tj` (для вакансий/резюме).

| # | Задача | Кто | Статус |
|---|---|---|---|
| 2б.1 | Подтвердить, что домен **ithona.tj** принадлежит компании и есть доступ к его DNS (сайт живёт на it-hona.tj — это разные домены; почту логично держать на ithona.tj, раз info@ithona.tj уже опубликован) | 🧑 | ☐ |
| 2б.2 | Почтовый сервис выбран: **Zoho Mail** (решение владельца, 2026-08) | 🧑 | ☑ |
| 2б.3 | Zoho Mail Admin Console (mailadmin.zoho.com): Domains → Add `ithona.tj` → подтвердить TXT-записью → Users → создать `info@` и `hr@` (hr@ — внутренний, см. 2б.6); включить 2FA обоим; catch-all не включать | 🧑 | ☐ |
| 2б.4 | DNS для Zoho (точные значения — в Admin Console, они зависят от дата-центра zoho.com/zoho.eu): **MX** `mx.zoho.com (10)`, `mx2.zoho.com (20)`, `mx3.zoho.com (50)`; **SPF** TXT `v=spf1 include:zohomail.com ~all`; **DKIM** — сгенерировать в Zoho (TXT `zmail._domainkey`); **DMARC** TXT `_dmarc` → `v=DMARC1; p=quarantine; rua=mailto:info@ithona.tj` | 🧑 | ☐ |
| 2б.5 | Проверка: письмо на info@ и hr@ извне доходит; ответ с них не попадает в спам (проверить на gmail.com); mail-tester.com ≥ 9/10 | 🧑 | ☐ |
| 2б.6 | `hr@ithona.tj` — **только для внутреннего использования** (решение владельца, 2026-08): на сайте, в JSON-LD и в переписке с клиентами НЕ публикуется. На сайте остаётся только `info@ithona.tj` — правки сайта не требуются | — | ☑ |

**Критерий готовности:** оба ящика принимают и отправляют почту, не попадая в спам; на сайте опубликован только info@.

**Важно про безопасность:** пароли от ящиков нигде не публиковать и не коммитить; для формы сайта почта не нужна (заявки идут через Telegram Worker / WhatsApp) — менять этап 1 не требуется.

---

## Этап 3 — Спринт доработок P3 (Claude делает в репозитории)

Цель: закрыть мелкие рекомендации из `15_BUGS_REMAINING.md`. Можно запускать параллельно с этапами 1–2.

| # | Задача | Источник | Кто | Статус |
|---|---|---|---|---|
| 3.1 | Доперевести на TJ/EN статичные строки: чипы услуг (services), списки систем (solutions), aria-label «Меню» (~30 ключей). Черновой перевод предложит Claude, владелец проверяет терминологию | R6 | 🤝 | ☐ |
| 3.2 | Контраст: затемнить серый #8b938f → ~#6d7672 для подписей; связать label форм через for/id | R10 | 🤖 | ☐ |
| 3.3 | Удалить из деплоя неиспользуемый `about-company.jpg` (330 KB); вычистить мёртвый код (renderForm/renderCallback в widget.js, футер-аккордеон в main.js) — с прогоном всех тестов | R4, R5 | 🤖 | ☐ |
| 3.4 | WebP-версии фото через `<picture>` (−30–50% веса) и/или self-host шрифта Inter (2 woff2 вместо CDN) | перф-резерв | 🤖 | ☐ |
| 3.5 | ~~Подтвердить владение доменом ithona.tj и работу почты~~ → перенесено и расширено в **этап 2б** (корпоративная почта: info@, hr@) | R9 | — | ☐ |

**Критерий готовности:** `npm test` зелёный после каждой правки; переключение TJ/EN не оставляет русских строк.

---

## Этап 4 — SEO для TJ/EN: отдельные URL (крупная задача, отдельное решение)

Сейчас поисковики индексируют только русскую версию (языки переключаются JS-ом). Решение — генерировать `/tj/` и `/en/` как настоящие страницы.

| # | Задача | Кто | Статус |
|---|---|---|---|
| 4.1 | Решение владельца: нужно ли SEO на таджикском/английском (если клиенты приходят по русским запросам — этап можно отложить) | 🧑 | ☐ |
| 4.2 | Если да: Claude пишет скрипт генерации — из существующих словарей `i18n.js` собираются статические `/tj/*.html`, `/en/*.html` (без фреймворков, тем же стеком) + настоящие hreflang + расширенный sitemap | 🤖 | ☐ |
| 4.3 | Тексты, которых нет в словарях (title/description для TJ/EN), — согласовать с владельцем | 🤝 | ☐ |
| 4.4 | Обновить e2e-тесты под три набора URL | 🤖 | ☐ |

**Оценка:** самый большой этап; трогает структуру сайта — только после стабилизации этапов 1–2.

---

## Этап 5 — Регулярная поддержка (постоянно)

| Задача | Кто |
|---|---|
| Перед каждым изменением сайта: `npm run check && npm run test:worker && npm run test:e2e` | 🤖/🧑 |
| Раз в неделю первое время: проверять чат заявок на спам → если появился, включить Turnstile (Claude добавит виджет в форму + проверку в Worker) | 🤝 |
| При изменении контента: обновлять `lastmod` в sitemap.xml только для изменённых страниц | 🤖 |
| При правках header/footer: помнить, что они дублируются в 6 файлах (или поручить Claude синхронизацию/миграцию на сборку — R3) | 🤝 |

---

## Порядок и зависимости

```
Этап 1 (Worker) ──┐
                  ├─→ Этап 2 (Production) ─→ Этап 5 (поддержка)
Этап 3 (P3-фиксы) ┘            │
                               └─→ Этап 4 (SEO TJ/EN — по решению)
```

Этапы 1 и 3 независимы и могут идти параллельно. Этап 2 лучше после 1 (чтобы задеплоить сайт сразу с подключённой формой). Этап 4 — только после стабильного продакшена.

## Что нужно от владельца прямо сейчас (минимум для старта)

1. Токен бота и chat_id — создать по шагам 1.1 (секреты никому не присылать в открытом виде — вносятся только в Cloudflare).
2. URL задеплоенного Worker — после него Claude подключит форму (1.4).
3. Решение по этапу 4 (SEO TJ/EN): делаем / откладываем.
4. Для 3.1 — готовность проверить таджикские переводы терминов.
