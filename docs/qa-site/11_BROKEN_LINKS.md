# Битые ссылки

## Результат: **битых ссылок нет (0 из 170)**

Методика (двойная проверка, закреплена в автотестах):

1. **Статический аудит** (`scripts/check-site.mjs`): каждый внутренний `href`/`src`/`srcset` из 6 страниц сверяется с файловой системой (с учётом `?v=`-параметров). Регистр путей проверяется как есть (Linux-совместимость: все пути в нижнем регистре, файлы совпадают байт-в-байт) — PASS.
2. **e2e** (`tests/e2e.spec.mjs`, «все внутренние ссылки со всех страниц отвечают 200»): собираются все `a[href]` со всех страниц, каждый уникальный внутренний URL запрашивается по HTTP → все 200 — PASS. Плюс переходы по каждому пункту меню и логотипу.

## Разбивка проверенных ссылок

| Тип | Количество (все страницы) | Статус |
|---|---|---|
| Внутренние навигационные (`*.html`, якоря) | 6 страниц × header(6)+footer(7)+CTA | все 200 / якоря существуют |
| `tel:+992889295555` | 3–4 на страницу | корректный формат E.164 |
| `mailto:info@ithona.tj` | 2 на страницу | корректный формат |
| Внешние (карты ×4, шрифты) | index, contacts | HTTPS, rel=noopener; доступность внешних сервисов из песочницы NOT TESTED |
| Ассеты (CSS/JS/img/favicon) | все страницы | все существуют, 200 |

## Несуществующие страницы

`/no-such-page.html` → HTTP 404 (тест-сервер). Создана брендовая `404.html` (noindex, ссылка на главную) — хостинг должен быть настроен отдавать её (см. 16_RELEASE_CHECKLIST.md).
